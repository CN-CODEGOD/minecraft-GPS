# Session Log
- Separated class definitions out of `minecraft-GPS.PSM1` into `graph.class.ps1` and `gps.class.ps1`, and updated scripts to dot-source the new class files.
- Pending work captured below to guide the next session.

# TODO
1. Add JSON map support (design data format and integrate load/save).
2. Test the project (unit scripts and integration flows).
3. Finish reconstructing the project structure after class extraction.
